# Retained v0.4 sparse-output failures

These notes are deliberately retained as negative regression artifacts. They passed early integration and traceability checks but failed the Final-template depth and content-recall contract.

Do not present them as accepted LitAnchor examples.
